# Cobra64 - Basic array operations test
# Tests: declaration, initialization, read, write

def main():
    # Byte array with initializer
    values: byte[] = [10, 20, 30, 40, 50]

    # Read array elements
    x: byte = values[0]
    y: byte = values[2]
    z: byte = values[4]

    # Write array elements
    values[1] = 25
    values[3] = 45

    # Sized array without initializer
    buffer: byte[10]
    buffer[0] = 1
    buffer[9] = 255
